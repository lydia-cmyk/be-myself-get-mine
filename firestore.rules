// Firestore security rules (production baseline)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/planner/{docId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
